// Incomplete type, but only properties that are needed for now
export interface SteamMap {
    id: string
    title: string
    preview: { 
        url: string
    }
    creator: {
        name: string
    }
}
